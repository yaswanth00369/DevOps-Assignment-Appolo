pipeline {
    agent any
    stages {
        stage('Test Webhook') {
            steps {
                echo "GitHub webhook triggered successfully!"
                sh 'date'
            }
        }
    }
}
